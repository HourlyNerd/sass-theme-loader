{
  "name": "sass-theme-loader",
  "version": "0.2.2",
  "description": "internal tool to keep track of all scss files and their deps",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf ./dist && mkdir dist && babel src --stage 0 --out-dir dist",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "webpack",
    "webpack-loader"
  ],
  "author": "misha koryak",
  "license": "MIT",
  "dependencies": {
    "glob": "6.0.4",
    "lodash": "3.10.1",
    "sander": "0.4.0"
  }
}
